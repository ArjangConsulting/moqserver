services:
  moqserver:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      # Point this at the .moqproj bundle you want to serve.
      - ./Tests/MoqFormatTests/Fixtures/sample-app.moqproj:/app/project.moqproj:ro
    command:
      - serve
      - --project
      - /app/project.moqproj
      - --hostname
      - "0.0.0.0"
      - --port
      - "8080"
