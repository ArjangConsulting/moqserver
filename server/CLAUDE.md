# CLAUDE.md

Legacy compatibility stub.

Agent instructions now live in one place:
- `AGENTS.md`

Use `AGENTS.md` as the single source of truth for server architecture, build/test commands, module structure, and code style.
