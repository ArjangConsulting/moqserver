# JitPack builds the Kotlin test-support client from clients/kotlin (the repo root is not a Gradle
# build). Consume it as com.github.ArjangConsulting:moqserver:<commit-sha>.
jdk:
  - openjdk17
install:
  - cd clients/kotlin && VERSION="$VERSION" ./gradlew publishToMavenLocal
